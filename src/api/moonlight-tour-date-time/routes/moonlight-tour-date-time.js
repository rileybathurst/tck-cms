'use strict';

/**
 * moonlight-tour-date-time router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::moonlight-tour-date-time.moonlight-tour-date-time');
