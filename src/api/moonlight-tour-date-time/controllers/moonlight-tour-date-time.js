'use strict';

/**
 * moonlight-tour-date-time controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::moonlight-tour-date-time.moonlight-tour-date-time');
