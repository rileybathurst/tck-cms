'use strict';

/**
 * sunset-tour-time controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sunset-tour-time.sunset-tour-time');
